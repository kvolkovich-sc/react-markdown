import React from 'react';
import { expect } from 'chai';
import { shallow } from 'enzyme';
import PlainMarkdownEditor from '.';

describe('<PlainMarkdownEditor />', () => {
  /* Recommended test-cases

  it('should have default props', () => {
    let component = <PlainMarkdownEditor />;
    expect(component.props.testProp).to.equal('Give me back my label!');
    expect(component.props.onClick).to.be.a('function');
  });
  it('should have the right class name', () => {
    let wrapper = shallow(<PlainMarkdownEditor className="test-class-name" />);
    expect(wrapper).to.have.className('plain-markdown-editor');
    expect(wrapper).to.have.className('test-class-name');
  });

  */
});
